#include<stdio.h>
#include"main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * Return: 1 computed value, 0 otherwise
 */
int _abs(int n)
{
if (n >= 0)
return (1);
else
return (0);
}
